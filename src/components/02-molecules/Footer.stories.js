import React from 'react';
import { Footer as Foo } from "./Footer.tsx"
// import { MenuCard as CardMenu } from "./MenuCard.tsx"

export default {
    title: 'Molecules/Footer',
    // tags: ['autodocs'],
};

export const Footer = (args) => <Foo {...args} />;