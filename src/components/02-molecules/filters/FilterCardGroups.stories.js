import { FilterCardGroups as Filter } from './FilterCardGroups.tsx';

const meta = {
    title: 'Molecules/Filters/FilterCardGroups',

    component: Filter,
    argTypes: {
        // nameImage: {
        //     control: {
        //         type: 'select',
        //         options: ['atom', 'exam', 'molecule', 'microscope']
        //     }
        // },
        // textCard: { control: 'text' },
    }
};



export default meta;

export const FilterCardGroups = {
    args: {
        // nameImage: 'atom',
        // textCard: 'Explora',
    }
};