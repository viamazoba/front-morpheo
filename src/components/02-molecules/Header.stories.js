import React from 'react';
import { Header as Head } from "./Header.tsx"
// import { MenuCard as CardMenu } from "./MenuCard.tsx"

export default {
    title: 'Molecules/Header',
    // tags: ['autodocs'],
};

export const Header = (args) => <Head {...args} />;